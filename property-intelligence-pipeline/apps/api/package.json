{
  "name": "@pip/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "seed": "tsx src/seed.ts"
  },
  "dependencies": {
    "@pip/crm": "*",
    "@pip/extraction": "*",
    "@pip/llm": "*",
    "@pip/matching": "*",
    "@pip/plaud": "*"
  }
}
